environments:
  - name: dev
    endpoints:
      production:
        url: http://mobilestore-dev.mycorp.com:4020
        url: http://mobilestore-dev.mycorp.com:4020
  - name: prod
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
