environments:
  - name: dev
    endpoints:
      production:
        url: http://mobilestore-dev.mycorp.com:4020/mobile-devices
      sandbox:
        url: http://mobilestore-dev.mycorp.com:4020/mobile-devices
  - name: prod
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
